/*
import { OPEN_DIALOG, CLOSE_DIALOG } from "../action/dialog";

const initialState = {
  isDialogOpen: false,
  dialogType: -1,
};

export default function dialogReducer(state = initialState, action) {
  switch (action.type) {
    case OPEN_DIALOG:
      return {
        ...state,
        isDialogOpen: true,
        dialogType: action.payload,
      };
    case CLOSE_DIALOG:
      return {
        ...state,
        isDialogOpen: false,
      };
    default:
      return state;
  }
}
*/
